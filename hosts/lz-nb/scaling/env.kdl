// Qt X11 scaling for 14" 2880×1800 @ 1.7x.
// Qt on Wayland ignores QT_SCALE_FACTOR (uses wp-fractional-scale-v1),
// so this only affects X11 Qt apps like WPS — unlike GDK_SCALE which
// also affects Wayland GTK apps.
environment {
    QT_SCALE_FACTOR "1.7"
    QT_FONT_DPI "163"  // 96 × 1.7
}
